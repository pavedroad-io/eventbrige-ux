<h2 mat-dialog-title>{{data.title}}</h2>

<mat-dialog-content>

      {{data.id}}

</mat-dialog-content>

<mat-dialog-actions>
  <input type="button" value="Confirm" class="mat-raised-button" [mat-dialog-close]="true">
  <input type="button" value="Cancel"  class="mat-raised-button" [mat-dialog-close]="false">
  <button class="mat-raised-button"(click)="cancel()"  [mat-dialog-close]="false">Cancel</button>
  <button class="mat-raised-button mat-primary"(click)="delete()" [mat-dialog-close]="true">Delete</button>
</mat-dialog-actions>
